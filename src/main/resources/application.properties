#logging.level.org.h2.server: DEBUG

# ===============================
# = DATA SOURCE
# ===============================
#security.enable-csrf=false
server.port=7333
#logging.level.org.springframework.web: DEBUG
#logging.level.org.hibernate: ERROR

# Set here configurations for the database connection
spring.datasource.driverClassName=com.mysql.jdbc.Driver
spring.datasource.url= jdbc:mysql://localhost:3222/flowzDB
spring.datasource.username = flowz
spring.datasource.password = flooowz

# Keep the connection alive if idle for a long time (needed in production)
spring.datasource.testWhileIdle = true
spring.datasource.validationQuery = SELECT 1

# Show or not log for each sql query
spring.jpa.show-sql = true

# Hibernate ddl auto (create, create-drop, update)
spring.jpa.hibernate.ddl-auto = update

# Naming strategy
spring.jpa.hibernate.naming-strategy = org.hibernate.cfg.ImprovedNamingStrategy

# Use spring.jpa.properties.* for Hibernate native properties (the prefix is
# stripped before adding them to the entity manager)

# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5Dialect

# Mail
spring.mail.host=email-smtp.us-west-2.amazonaws.com
spring.mail.username=AKIAI4TBIAMXT3SQTULQ
spring.mail.password=Ava5b1Klt0F0b75GcUrnmXhkI0oPJLWcyd7zklqZ5jsZ
spring.mail.port=25
spring.mail.properties.mail.setup.starttls.enable=true